---

- name: "Apply AppArmor MAC hardening (Ubuntu/Debian family)"
  when: "linux_apparmor_disabled is not defined or not linux_apparmor_disabled | bool"
  ansible.builtin.import_tasks: "tasks.yml"
  tags: "linux_apparmor_ubuntu"

...

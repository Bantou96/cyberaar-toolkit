---
- name: "Apply PAM authentication hardening (Ubuntu/Debian family)"
  when: "linux_authselect_disabled is not defined or not linux_authselect_disabled | bool"
  ansible.builtin.import_tasks: "tasks.yml"
  tags: "linux_authselect_ubuntu"

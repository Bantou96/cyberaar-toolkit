---

# =============================================================================
#  CyberAar | Main Playbook Orchestrator
#  Run: ansible-playbook 0_execute_full_pipeline.yml -i inventory/hosts.yml
# =============================================================================

- name: "CyberAar | Step 1 — Pre-Hardening Baseline"
  ansible.builtin.import_playbook: 1_execute_baseline_before.yml
  tags: [baseline, before]

- name: "CyberAar | Step 2 — System Hardening"
  ansible.builtin.import_playbook: 2_configure_hardening.yml
  tags: [hardening]

- name: "CyberAar | Step 3 — Post-Hardening Baseline"
  ansible.builtin.import_playbook: 3_execute_baseline_after.yml
  tags: [baseline, after]

...
